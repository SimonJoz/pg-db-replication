# ==============================================================================
# Logs massage into the $LOG_FILE.
# ==============================================================================

log() {
  local message="$1"
  echo "[$(date +"%Y-%m-%d %H:%M:%S")]: $message" | tee -a "$LOG_FILE"
}
